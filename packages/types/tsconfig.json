{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true,
    "emitDeclarationOnly": true, // only emit .d.ts since no runtime needed
    "composite": true            // so project references work
  },
  "include": ["src"]
}
